const base = "/react-spotify-clone";

export const routes = {
  HOME: base,
  SEARCH: `${base}/search`,
//   PLAYLIST: `${base}/playlist`,
};

export const paths = {
  HOME: routes.HOME,
//   LOGIN: routes.LOGIN,
//   PLAYLIST: `${routes.PLAYLIST}/:playlistId`,
};